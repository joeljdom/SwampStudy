declare module '*.svg';
declare module '*.png';
declare module 'react/jsx-runtime';

// Provide simple React module declarations if @types/react is not installed yet.
// These are minimal fallbacks; installing @types/react is preferred.
// Keep only asset + runtime module declarations here.
