{
  "name": "swampstudy-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server run start\" \"npm --prefix client run dev\"",
    "start:server": "npm --prefix server start",
    "client:dev": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
